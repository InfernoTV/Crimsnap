# Changelog

## 0.1.0

- Added signed in-app update checks with changelog preview, passive install progress, and restart prompt.
- Added Snap screen selection fixes so the actual-screen picker shows reliably above the main app.
- Added window-specific region capture, audio device selection, friendly monitor names, export/import config, quality presets, always-on-top pinning, and public release packaging.
